# 客服共情副驾上下文地图

本项目在现有通用 Agent 平台上增加美妆电商客服垂直能力。客服业务事实、接待辅助结论、风险闭环和 Agent 运行事实分别拥有自己的语言和状态，不共享领域实体。

## 上下文

- [服务数据](./docs/domain/service-data/CONTEXT.md)：保存从客服、订单和售后来源导入的只读业务事实，并形成消费者服务轨迹。
- [接待辅助](./docs/domain/service-assistance/CONTEXT.md)：基于一次不可变的上下文快照生成分析、回复草稿和处置建议，并记录客服的采纳决定。
- [风险运营](./docs/domain/risk-operations/CONTEXT.md)：把风险信号归并为可分派、可跟踪、可关闭的风险预警。
- [Agent 平台](./docs/domain/agent-platform/CONTEXT.md)：管理 Agent、工作流、模型、工具、知识、运行记录和评测，不拥有客服业务事实。

现有店铺、点评、博客、优惠券等本地生活能力属于遗留业务上下文。比赛垂直链路不复用其领域实体，也不要求在本阶段删除它们。

## 关系

- **服务数据 -> 接待辅助**：服务数据通过只读应用接口提供上下文快照；接待辅助不能修改导入事实。
- **接待辅助 -> Agent 平台**：接待辅助创建版本化 Agent Run，并消费结构化运行结果；Agent 平台只保存运行证据和版本快照。
- **接待辅助 -> 风险运营**：接待辅助提交经过校验的风险信号；风险运营负责去重、定级和生命周期转换。
- **风险运营 -> 接待辅助**：风险运营提供当前有效预警，使新建议不会重复承诺或遗漏既有升级状态。
- **Agent 平台 -> 外部 AI 能力**：Python 推理服务、模型供应商和其他外部能力都通过受控适配器接入，无权直接访问或修改领域表。

## 依赖规则

1. `com.hmdp.ai` 不依赖任何客服领域包。
2. 服务数据不依赖接待辅助、风险运营或 Agent 平台。
3. 接待辅助只通过公开应用接口或端口读取服务数据和风险状态。
4. 风险运营不读取模型原始响应，只接收符合领域契约的风险信号。
5. 跨上下文只传递 ID、值对象、事件和稳定 DTO，不传递 ORM 实体或 Repository。
6. 写路径禁止跨上下文直接联表；工作台查询由专用查询服务组合各上下文的只读结果。
