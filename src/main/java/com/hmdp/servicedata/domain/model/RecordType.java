package com.hmdp.servicedata.domain.model;

/** Normalized record kinds produced by the workbook parser and staged for import. */
public enum RecordType {
    CONSUMER_ALIAS,
    CONVERSATION,
    MESSAGE,
    ORDER_SNAPSHOT,
    SERVICE_CASE,
    SOURCE_LINK
}
