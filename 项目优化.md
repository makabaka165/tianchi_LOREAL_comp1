# 项目优化

## AI 店铺分析模块当前落地状态

本轮已经先落地方向一和方向二，并继续推进 P3：AI 不再作为 `ShopSummaryService` 内部的若干方法存在，而是迁移为应用服务 + 编排中心 + 显式工作流，同时把核心响应升级为结构化 payload、Typed Evidence、治理观测和评价 RAG 混合召回。

当前链路：

```text
ShopSummaryController
  -> ShopAIApplicationService
      -> ShopAIOrchestrator
          -> ChatWorkflow
          -> SummaryWorkflow / QualitySummaryWorkflow
          -> QAWorkflow
          -> CompareWorkflow
          -> RecommendWorkflow
          -> ModelGateway / MemoryService / QualityGuard / FallbackPolicy
```

已完成的架构收敛：

- `ShopSummaryService` 旧兼容门面已删除，Controller 不再直接依赖 `ShopAIService`。
- 核心总结、问答、对比、推荐不再依赖 Agent 自动 Tool Calling，而是走显式 Workflow。
- `ShopAIService` 已降级为底层模型适配接口，仅保留工作流需要的模型调用能力。
- `/api/shop-summary/ai/chat` 采用规则 + LLM 意图识别 + slot memory 的路由模式，参数不足时先澄清。
- 普通自由对话不再暴露 `ShopTool`，只做能力说明、参数补充和低风险回答。
- 同步 AI 接口统一返回 `ShopAIResponse` 元信息和结构化 payload：`summary`、`qa`、`compare`、`recommend`、`chat` 五选一。
- 旧顶层字段 `response/answer/comparison/recommendations/usedTools/winnerByAspect/analysis` 已从 `ShopAIResponse` 删除。
- 证据从旧 `ReviewEvidence` 升级为 `EvidenceItem`，支持 `REVIEW` 和 `SHOP_PROFILE` 两种类型，模型引用统一使用 `review:{blogId}`、`shop_profile:{shopId}` 这类字符串证据 ID。
- QA、对比、推荐 prompt 已升级到 v3 严格 JSON 输出，并由 `ModelGateway` 解析成 DTO；解析失败会先 repair，再进入结构化 fallback。
- `QualityGuard` 已增加结构化校验：证据覆盖、对比胜出枚举、推荐候选店铺合法性、rank 去重等。
- P3.2 已接入 AI 治理观测：`AiMetricsService` 写入 Micrometer/Prometheus，同时保留内存 snapshot；`ModelGateway` 记录模型调用耗时和估算 token；`ShopAIOrchestrator` 记录端到端请求耗时。
- P3.2 已增加 prompt 灰度治理：`PromptTemplateRender` 返回实际 `content/version/variant`；`PromptVersionPolicy` 按 `userId + intent + routeKey` 稳定 hash 选择 stable/canary，默认 canary 关闭。
- P3.3 已增加评价 RAG 混合召回：评价发布事件追加向量索引，问答/对比/推荐证据检索先走 DB 规则召回，再叠加向量召回，并用确定性 rerank 排序。
- P3.3 已增加管理端 RAG 回补入口：`/api/shop-summary/admin/rag/shops/{shopId}/rebuild` 和 `/api/shop-summary/admin/rag/rebuild`，权限为 `ai:rag:manage`。
- 异步 AI 任务系统 Phase 1 已落地：管理端 RAG 全量/单店重建支持提交任务后返回 `taskId`，后台 worker 执行，前端通过轮询查询状态。
- 平台 FAQ/政策 RAG 与店铺评价 RAG 已拆分为独立索引，核心工作流不再依赖隐式 `contentRetriever` 旁路。
- AI 入口已增加用户级配额，默认每用户每分钟 10 次、每天 200 次；全局模型 RateLimiter 继续作为系统保护。
- 质量门已收窄为结构化校验优先，避免“店内禁止吸烟”这类正常业务描述触发误杀。
- `GET /api/shop-summary/{shopId}/quality` 已改为无写记忆副作用，写 summary memory 使用 `POST /api/shop-summary/{shopId}/quality/with-memory`。
- `/api/shop-summary/ai/chat/stream` 已增加流式结果事后审计事件，完整文本不通过质量检查时会打标并记录质量拒绝指标。
- RAG 启动期已移除 embedding API 探测硬依赖；Redis Stack 不可用仍按生产配置 fail fast，embedding 服务抖动只导致导入告警。
- 店铺总结记忆固定写入 `shop:summary:{shopId}:{userId}`，不会和通用 chat memory 串号。
- 降级或低置信总结不写入 summary memory，避免后续 QA 使用污染上下文。
- 店铺评价变化会清理 AI 结果缓存、统计缓存和对应店铺 summary memory。
- RAG Redis Stack 默认生产 fail fast；仅 dev/test 显式设置 `rag.redis.fallback-to-memory=true` 时允许内存向量库回退。

下一阶段仍未完成的方向：

- 方向三：完整 RAG 检索系统。当前已完成评价规则 + 向量混合召回和确定性 rerank，后续还可升级为评价清洗、分句切片、主题标签、专用 reranker 模型和主题聚类。
- 方向四：异步 AI 任务系统。当前已完成管理端 RAG 重建 Phase 1；长分析、批量总结、运营报表后续仍可继续扩展为 taskId + 后台执行 + SSE/WebSocket 推送。
- 方向五：独立 AI 服务。当前仍保持单体内 AI 编排层，后续可拆出 `hmdp-ai-service` 以支持独立扩容、灰度、监控和模型治理。
- P3 后续治理：异步 AI task 已完成管理端 RAG Phase 1；独立 AI 服务、真实模型 reranker、向量索引物理删除/压缩治理仍未完成。

## 1. 登录鉴权模块当前定位

当前项目的登录鉴权模块已经从最初的“验证码登录 + Redis token + 拦截器判断登录态”，升级为以 Sa-Token 为核心、RBAC 为权限基础、登录审计和风险控制为辅助能力的一阶段企业级认证授权体系。

这次改造并不是简单替换登录方式，而是围绕真实企业项目常见的几个关键问题进行补强：

- 用户是否登录，由专业认证框架 Sa-Token 统一维护。
- 用户能访问什么接口，由 RBAC 角色权限模型统一描述。
- 旧业务代码仍依赖 `UserHolder`，通过兼容层平滑过渡。
- 后台管理员可以管理用户状态、角色绑定、商家店铺归属。
- 登录失败、验证码错误、封禁、登出等行为可以被审计。
- 连续失败登录会进入风险控制流程，避免验证码接口和登录接口被滥用。
- 接口失败响应开始具备统一错误码，便于前端和后续网关、监控、告警系统识别。

整体目标是让认证授权模块不再像课程设计中的简单登录判断，而是具备企业项目中常见的“认证、授权、审计、风控、后台治理、平滑迁移”能力。

## 2. 改造前的问题

早期登录鉴权实现主要依赖如下机制：

- `/user/code` 发送验证码。
- `/user/login` 校验验证码。
- 登录成功后手动生成 UUID token。
- 将 token 对应的用户信息写入 Redis。
- `RefreshTokenInterceptor` 从 Redis 中刷新登录态。
- `LoginInterceptor` 判断 `UserHolder` 中是否存在用户。
- 业务层通过 `UserHolder.getUser().getId()` 获取当前用户。

这种实现适合教学项目，但在企业落地中存在明显问题：

- token 生命周期、踢人下线、并发登录、共享登录态等能力都需要自己维护。
- 权限控制只能做到“是否登录”，很难细分到角色和权限码。
- 用户、商家、管理员缺少明确的权限边界。
- 登录失败、验证码错误、登出、后台操作缺少审计链路。
- 无统一错误码，前端只能依赖错误文案判断失败原因。
- 无用户禁用机制，被禁用用户可能继续使用旧 token。
- 商家权限只有 `shop:update:own` 这类权限码，但缺少“是不是自己的店铺”的数据模型支撑。
- 后台缺少用户禁用、角色绑定、商家店铺归属维护等管理能力。

因此，本轮优化的核心不是“换一个登录框架”，而是补齐认证授权模块在企业项目中的基础治理能力。

## 3. 新的总体架构思路

当前登录鉴权模块采用如下分层思路：

```text
客户端
  |
  | authorization: Bearer <token>
  v
Sa-Token 认证层
  |
  | 解析 token，确认是否登录
  v
UserHolder 兼容层
  |
  | 根据 Sa-Token loginId 查询用户基础信息
  | 填充 UserHolder，兼容旧业务代码
  v
Controller 权限注解
  |
  | @SaCheckLogin
  | @SaCheckPermission
  v
StpInterface 权限桥接
  |
  | 调用 PermissionService
  v
RBAC 数据表
  |
  | sys_role
  | sys_permission
  | sys_user_role
  | sys_role_permission
  v
业务服务层
  |
  | CurrentUserService 获取当前用户
  | MerchantShopService 判断商家归属
  v
审计与风控
  |
  | sys_login_log
  | sys_operation_log
  | Redis 登录失败计数和临时封禁
```

这个架构的关键点是：

- Sa-Token 只负责认证和权限框架能力。
- RBAC 表负责定义用户、角色、权限之间的关系。
- `PermissionService` 负责把数据库权限转换成应用层可使用的权限列表。
- `StpInterfaceImpl` 是 Sa-Token 和项目 RBAC 权限体系之间的桥。
- `SaTokenUserHolderInterceptor` 负责让旧的 `UserHolder` 逻辑继续可用。
- `CurrentUserService` 是后续业务层逐步替换 `UserHolder` 的统一入口。
- `sys_login_log` 和 `sys_operation_log` 分别记录登录审计和后台操作审计。
- Redis 用于验证码、限流、登录失败计数和临时封禁。

## 4. Sa-Token 登录认证改造

### 4.1 登录成功后由 Sa-Token 维护登录态

登录成功后，不再把手写 UUID token 作为主要登录态，而是调用：

```java
StpUtil.login(user.getId());
String token = StpUtil.getTokenValue();
```

这样登录态由 Sa-Token 统一管理，项目可以直接获得：

- token 生成。
- token 解析。
- 登录状态判断。
- 登出。
- 踢人下线。
- 多端登录控制。
- Redis 持久化登录态。
- 注解式权限校验。

当前前端仍使用 `authorization` 请求头，并且兼容 `Bearer` 前缀。

### 4.2 登出改造

登出接口调用：

```java
StpUtil.logout();
```

同时清理：

- Sa-Token 登录态。
- 当前线程中的 `UserHolder`。
- 登录审计中的登出记录。

这样登出不再依赖旧的 `login:token:{token}` Redis 结构。

### 4.3 旧拦截器体系拆除

旧的 `LoginInterceptor` 和 `RefreshTokenInterceptor` 已经不再作为主认证链路使用。

当前请求链路中保留的是：

- `SaInterceptor`：负责 Sa-Token 注解和登录态处理。
- `SaTokenUserHolderInterceptor`：负责兼容旧业务代码中的 `UserHolder`。

这意味着认证中心已经切换到 Sa-Token，旧 Redis token 体系已完成拆除。

## 5. UserHolder 兼容层设计

项目中很多历史业务代码仍然依赖：

```java
UserHolder.getUser().getId()
```

如果一次性全部替换，改动范围会很大，也容易引入业务回归。因此当前采用兼容层方案：

```java
if (StpUtil.isLogin()) {
    Long userId = StpUtil.getLoginIdAsLong();
    User user = userService.getById(userId);
    UserHolder.saveUser(BeanUtil.copyProperties(user, UserDTO.class));
}
```

请求结束后：

```java
UserHolder.removeUser();
```

这样有几个好处：

- 新认证体系由 Sa-Token 接管。
- 旧业务代码暂时无需大规模重构。
- 后续可以逐步把 `UserHolder` 替换为 `CurrentUserService`。
- 迁移过程风险更小。

同时，本轮增强还在兼容层中增加了用户禁用校验：

- 如果用户 `status = 0`，说明账号被禁用。
- 兼容层会调用 `StpUtil.logout(userId)` 清理登录态。
- 返回统一错误码 `ACCOUNT_DISABLED`。

这避免了“用户已被后台禁用，但旧 token 仍可继续访问”的问题。

## 6. RBAC 权限模型设计

### 6.1 角色模型

当前系统只保留三类角色：

| 角色 | role_key | 含义 |
| --- | --- | --- |
| 买家 | buyer | 普通用户，可浏览、点赞、关注、秒杀、使用 AI 聊天 |
| 商家 | merchant | 可维护自己的店铺和优惠券 |
| 管理员 | admin | 可访问后台管理能力 |

角色表为：

```text
sys_role
```

关键字段：

- `id`
- `role_key`
- `role_name`
- `status`
- `remark`
- `create_time`
- `update_time`

其中 `role_key` 有唯一索引，避免重复角色。

### 6.2 权限模型

权限表为：

```text
sys_permission
```

权限码示例：

- `shop:read`
- `blog:create`
- `voucher:seckill`
- `shop:update:own`
- `voucher:create:own`
- `document:manage`
- `ai:memory:manage`
- `system:log:read`
- `role:assign`

权限码的设计思路是：

```text
资源:动作
资源:动作:范围
```

例如：

- `shop:update` 表示管理员可以更新任意店铺。
- `shop:update:own` 表示商家只能更新自己的店铺。
- `voucher:create:own` 表示商家只能给自己的店铺创建优惠券。

### 6.3 用户角色关联

用户和角色之间通过：

```text
sys_user_role
```

进行绑定。

关键约束：

- `user_id`
- `role_id`
- `status`
- `user_id + role_id` 唯一索引

`status` 的意义：

- `1`：绑定启用。
- `0`：绑定禁用。

这样后台不需要物理删除角色绑定，可以通过启停保留历史关系。

### 6.4 角色权限关联

角色和权限之间通过：

```text
sys_role_permission
```

进行绑定。

关键约束：

- `role_id`
- `permission_id`
- `status`
- `role_id + permission_id` 唯一索引

这样可以避免重复授权，并支持权限启停。

## 7. PermissionService 权限服务

当前提供了统一的权限查询服务：

```java
List<String> getRoleKeysByUserId(Long userId);

List<String> getPermissionCodesByUserId(Long userId);

boolean hasRole(Long userId, String roleKey);

boolean hasPermission(Long userId, String permissionCode);

void assignDefaultBuyerRole(Long userId);

void assignRole(Long userId, String roleKey);

void setUserRoleStatus(Long userId, String roleKey, Integer status);
```

设计重点：

- 所有角色和权限查询都通过数据库 RBAC 表完成。
- 新注册用户默认绑定 `buyer`。
- 后台可以给用户分配 `buyer`、`merchant`、`admin`。
- 后台可以启用或禁用某个用户的角色绑定。
- 查询权限时只返回启用状态的角色和权限。

这部分是后续继续接入更复杂后台权限管理的基础。

## 8. StpInterface 权限桥接

Sa-Token 的权限体系通过 `StpInterfaceImpl` 接入项目 RBAC。

核心逻辑：

```java
public List<String> getRoleList(Object loginId, String loginType) {
    Long userId = parseUserId(loginId);
    return permissionService.getRoleKeysByUserId(userId);
}

public List<String> getPermissionList(Object loginId, String loginType) {
    Long userId = parseUserId(loginId);
    return permissionService.getPermissionCodesByUserId(userId);
}
```

这样 Controller 上的注解：

```java
@SaCheckPermission("voucher:seckill")
```

最终会走到数据库中的权限表。

对于管理员角色，当前也兼容了 `*` 权限，方便后台管理类接口扩展。

## 9. 接口权限控制方式

当前项目采用注解式权限控制。

### 9.1 登录接口

只要求登录即可访问：

```java
@SaCheckLogin
```

适合：

- `/user/me`
- 签到
- 关注
- 点赞
- 个人中心

### 9.2 权限接口

要求具备某个权限码：

```java
@SaCheckPermission("voucher:seckill")
```

适合：

- 秒杀下单。
- 文档管理。
- AI 记忆管理。
- 商家店铺维护。
- 商家优惠券维护。

### 9.3 多权限 OR 模式

例如店铺更新：

```java
@SaCheckPermission(value = {"shop:update:own", "shop:update"}, mode = SaMode.OR)
```

含义：

- 商家拥有 `shop:update:own`，但还需要判断店铺归属。
- 管理员拥有 `shop:update`，可以更新任意店铺。

权限注解只解决“有没有入口权限”，真正的业务归属还要在 Service 层判断。

## 10. 商家店铺归属模型

之前只有权限码：

```text
shop:update:own
voucher:create:own
```

但没有数据模型说明“哪个店属于哪个商家”。

现在新增：

```text
sys_merchant_shop
```

关键字段：

- `merchant_user_id`
- `shop_id`
- `status`
- `remark`
- `create_time`
- `update_time`

唯一约束：

```text
merchant_user_id + shop_id
```

这样可以支持：

- 一个商家绑定多个店铺。
- 一个店铺可以绑定商家。
- 绑定关系可以启用或禁用。
- 后台可以维护绑定和解绑。

业务层通过：

```java
merchantShopService.isShopOwner(merchantUserId, shopId)
```

判断商家是否有权操作某个店铺。

这使 `shop:update:own` 不再只是一个权限码，而是具备真实业务约束。

## 11. 商家字段级控制

商家更新自己的店铺时，并不是所有字段都可以改。

当前商家允许修改：

- 店铺名称。
- 店铺类型。
- 图片。
- 商圈。
- 地址。
- 经纬度。
- 均价。
- 营业时间。

商家不允许修改：

- 销量 `sold`。
- 评论数 `comments`。
- 评分 `score`。

原因是这些字段属于平台统计数据或用户行为结果，不应该由商家直接修改。

当前实现思路是：

```java
if (!admin) {
    shop = buildMerchantAllowedUpdate(shop);
}
```

即普通商家提交的数据会被重新组装，只保留允许更新的字段。

这比单纯依赖前端隐藏字段更安全，因为后端做了字段级白名单控制。

## 12. Admin 后台能力增强

当前后台 RBAC Controller 已经支持以下能力。

### 12.1 用户分页查询

接口支持：

- 分页参数。
- 手机号模糊查询。
- 用户状态筛选。
- 返回用户已启用角色列表。

用途：

- 后台用户管理。
- 查找某个手机号用户。
- 查看账号状态。
- 查看用户当前角色。

### 12.2 用户禁用与启用

后台可以修改：

```text
tb_user.status
```

状态含义：

- `1`：启用。
- `0`：禁用。

禁用用户时：

- 更新数据库状态。
- 调用 `StpUtil.logout(userId)` 清理登录态。
- 写入操作审计日志。

这样可以实现后台封禁用户。

### 12.3 用户角色绑定启停

后台可以对某个用户的某个角色进行启用或禁用：

```text
sys_user_role.status
```

这样比删除绑定更适合企业项目，因为：

- 可以保留绑定历史。
- 可以快速恢复角色。
- 后续可以扩展操作记录和审批流。

### 12.4 操作日志分页查询

后台可以查询：

```text
sys_operation_log
```

支持筛选：

- 模块。
- 操作类型。
- 操作人。
- 成功或失败。
- 分页参数。

这为后台审计、问题排查和安全追踪提供基础能力。

### 12.5 商家店铺绑定与解绑

后台可以管理：

```text
sys_merchant_shop
```

支持：

- 查询绑定列表。
- 绑定商家和店铺。
- 解绑商家和店铺。
- 记录操作日志。

这使商家管理自己的店铺有了完整的数据基础。

## 13. 登录审计增强

登录审计表：

```text
sys_login_log
```

现在记录的信息包括：

- `user_id`
- `phone`
- `login_type`
- `action`
- `success`
- `fail_reason`
- `ip`
- `user_agent`
- `device_fingerprint`
- `token_id`
- `login_time`
- `logout_time`
- `risk_level`
- `fail_count`
- `status`
- `create_time`
- `update_time`

相比之前，新增重点是：

- 设备指纹。
- 风险等级。
- 失败次数。

这样可以回答几个企业项目中常见的问题：

- 哪个手机号频繁登录失败？
- 是哪个 IP 或设备在不断尝试？
- 当前失败是否已经达到高风险？
- 哪些用户被风控拦截？
- 某次登录失败具体原因是什么？

## 14. 设备指纹设计

当前支持从请求头读取：

```text
X-Device-Fingerprint
Device-Fingerprint
```

如果前端没有传设备指纹，则后端使用：

```text
IP + User-Agent
```

生成 SHA-256 摘要作为兜底设备指纹。

这样做的优点：

- 当前没有完整前端设备指纹 SDK 时，也能进行基本风控。
- 后续可以平滑替换为更专业的设备指纹方案。
- 风控维度不只依赖手机号，也不只依赖 IP。

后续企业级方案可以接入：

- 前端设备指纹 SDK。
- App 设备 ID。
- 浏览器指纹。
- 风控平台。
- 网关层风控。

## 15. 登录失败次数封禁

当前登录失败计数使用 Redis。

核心 key：

```text
login:fail:count:{phone}:{deviceFingerprint}
login:block:{phone}:{deviceFingerprint}
```

当前策略：

- 验证码为空、过期、错误都算登录失败。
- 失败次数在一定窗口期内累计。
- 达到 5 次后临时封禁。
- 封禁期间即使验证码正确也不能登录。
- 登录成功后清理失败计数和封禁状态。

风险等级：

| 失败次数 | 风险等级 |
| --- | --- |
| 1-2 次 | 低风险 |
| 3-4 次 | 中风险 |
| 5 次及以上 | 高风险 |

这是一套轻量实现，但已经具备企业项目中的基础风控思想：

- 失败计数。
- 时间窗口。
- 临时封禁。
- 风险分级。
- 审计记录。

后续可以继续扩展为：

- 按 IP 维度封禁。
- 按手机号维度封禁。
- 按设备维度封禁。
- 按用户 ID 维度封禁。
- 黑名单和白名单。
- 动态验证码策略。
- 图形验证码升级校验。
- 接入 Bucket4j 或网关限流。

## 16. 验证码一次性使用

验证码校验采用 Redis Lua 脚本保证原子性：

```text
读取验证码
如果不存在，返回过期
如果不匹配，返回错误
如果匹配，删除验证码
返回成功
```

这样可以保证：

- 验证码只能使用一次。
- 校验和删除是原子操作。
- 避免并发请求重复使用同一个验证码。

这是比普通 Java 代码先查再删更安全的实现方式。

## 17. 验证码发送限流

验证码发送仍使用 Redis 实现限流：

- 单手机号冷却时间。
- 单手机号每日发送上限。
- 单 IP 分钟级请求上限。

当前项目没有真实短信服务，因此验证码仍通过日志打印用于测试。

后续如果要集群部署，可以考虑：

- 当前 Redis 计数方案已经天然支持多实例共享状态。
- 如果需要更标准的令牌桶限流模型，可以引入 Bucket4j。
- 如果项目上网关，可以优先在网关层做 IP 和接口级限流。
- 应用层保留手机号和业务维度限流。

建议的企业级限流分层：

```text
网关层：IP、URI、User-Agent、全局 QPS
应用层：手机号、用户 ID、设备指纹、业务行为
短信服务层：供应商频率和模板限制
```

## 18. 统一错误码体系

新增：

```java
ErrorCode
BusinessException
```

`Result` 新增：

```java
private Integer code;
```

成功响应：

```json
{
  "success": true,
  "code": 0,
  "data": {}
}
```

失败响应：

```json
{
  "success": false,
  "code": 40100,
  "errorMsg": "Not logged in"
}
```

当前错误码包括：

| 错误码 | 含义 |
| --- | --- |
| `0` | 成功 |
| `40000` | 参数错误 |
| `40010` | 验证码错误 |
| `40011` | 验证码过期 |
| `40100` | 未登录 |
| `40300` | 权限不足 |
| `40310` | 账号禁用 |
| `42300` | 登录临时封禁 |
| `42900` | 请求过于频繁 |
| `40400` | 资源不存在 |
| `50000` | 业务错误 |
| `50001` | 系统异常 |

统一错误码的价值：

- 前端不用依赖中文错误文案判断失败原因。
- 后续可以接入统一异常监控。
- 可以统计不同错误码出现频率。
- 网关或客户端可以针对特定错误码做跳转或提示。
- 对外接口更规范。

## 19. 全局异常处理

全局异常处理通过：

```java
WebExceptionAdvice
```

统一处理：

- 未登录异常。
- 权限不足异常。
- Sa-Token 异常。
- 业务异常。
- 参数异常。
- 未知运行时异常。

这样 Controller 和 Service 中可以更专注业务逻辑，不需要到处手动拼失败响应。

## 20. 操作审计设计

后台操作审计表：

```text
sys_operation_log
```

记录字段：

- `operator_user_id`
- `module`
- `operation`
- `target_type`
- `target_id`
- `detail`
- `success`
- `fail_reason`
- `ip`
- `user_agent`
- `operation_time`
- `create_time`

当前已经接入的操作包括：

- 分配角色。
- 启用或禁用用户角色。
- 禁用或启用用户。
- 绑定商家店铺。
- 解绑商家店铺。

这部分让后台管理不再是“黑盒操作”，而是可以追踪：

- 谁操作的。
- 操作了什么模块。
- 操作对象是谁。
- 成功还是失败。
- 失败原因是什么。
- 从哪个 IP 和 User-Agent 发起。

## 21. 数据库变更

本轮新增 migration：

```text
src/main/resources/db/migration/V20260608_05_admin_risk_controls.sql
```

主要变更：

```sql
ALTER TABLE `tb_user`
  ADD COLUMN `status` tinyint(1) NOT NULL DEFAULT 1 COMMENT '1 enabled, 0 disabled' AFTER `icon`;

ALTER TABLE `sys_login_log`
  ADD COLUMN `device_fingerprint` varchar(128) DEFAULT NULL COMMENT 'Device fingerprint' AFTER `user_agent`,
  ADD COLUMN `fail_count` int NOT NULL DEFAULT 0 COMMENT 'Failure count in risk window' AFTER `risk_level`,
  ADD KEY `idx_sys_login_log_device` (`device_fingerprint`) USING BTREE;
```

同时更新了初始化脚本：

```text
src/main/resources/db/hmdp.sql
```

保证新环境初始化数据库时也包含这些字段。

## 22. MockMvc 测试覆盖

新增测试：

```text
src/test/java/com/hmdp/AuthAdminMockMvcTest.java
```

测试方式：

- `SpringBootTest`
- `AutoConfigureMockMvc`
- 真实 Spring 上下文
- 真实 MyBatis-Plus Service
- 真实 Redis 和 MySQL 环境

覆盖内容：

- 登录验证码错误返回统一错误码。
- 连续失败达到阈值后返回临时封禁错误码。
- 登录日志记录设备指纹、风险等级、失败次数。
- admin 可以禁用用户。
- 被禁用用户旧 token 不能继续访问。
- admin 可以禁用和启用用户角色绑定。
- admin 可以绑定商家和店铺。
- admin 可以解绑商家和店铺。
- admin 可以分页查询操作日志。

验证命令：

```bash
mvn -q -DskipTests compile
mvn -q -Dtest=AuthAdminMockMvcTest test
```

## 23. 当前完成度判断

从登录鉴权模块角度看，当前已经完成了比较完整的一阶段企业级改造。

已经具备：

- Sa-Token 认证。
- Redis 持久化 token。
- RBAC 数据模型。
- 权限注解。
- 角色权限服务。
- 用户默认角色。
- admin 后台角色管理基础能力。
- 用户禁用。
- 角色绑定启停。
- 商家店铺归属模型。
- 商家自有资源权限判断。
- 字段级更新控制。
- 登录审计。
- 操作审计。
- 设备指纹。
- 登录失败次数封禁。
- 统一错误码。
- MockMvc 回归测试。

因此，这部分已经不只是“能登录”，而是形成了一个可以继续扩展的认证授权子系统。

## 24. 后续可继续优化方向

虽然当前已经形成闭环，但如果继续贴近企业真实项目，还可以进一步增强。

### 24.1 权限缓存

当前权限查询直接查数据库。

后续可以缓存：

- 用户角色列表。
- 用户权限列表。

缓存位置：

- Redis。
- Caffeine 本地缓存。
- Redis + 本地缓存二级缓存。

注意点：

- 角色变更后要清理缓存。
- 用户禁用后要清理 token 和权限缓存。
- admin 角色变更应强制重新登录或清理权限缓存。

### 24.2 更细的错误码规范

当前错误码已经起步。

后续可以按模块拆分：

```text
10xxx 用户模块
20xxx 店铺模块
30xxx 优惠券模块
40xxx 权限模块
50xxx AI 模块
```

并沉淀错误码文档，方便前后端协作。

### 24.3 登录风控策略配置化

当前失败阈值写在常量中：

```java
LOGIN_FAIL_LIMIT = 5
LOGIN_FAIL_WINDOW_MINUTES = 15
LOGIN_BLOCK_MINUTES = 15
```

后续可以放入：

- `application.yaml`
- Nacos
- Apollo
- 数据库配置表

这样可以不发版调整风控策略。

### 24.4 更完整的后台 RBAC 管理

后续可以继续补：

- 角色列表分页。
- 权限列表分页。
- 角色权限绑定接口。
- 用户角色批量分配。
- 操作日志导出。
- 登录日志分页查询。
- 用户封禁原因和封禁时间。
- 用户解封接口。

### 24.5 接入真实短信服务

当前验证码通过日志打印测试。

后续可以接入：

- 阿里云短信。
- 腾讯云短信。
- 华为云短信。
- 自建短信网关。

并增加：

- 短信发送记录。
- 供应商失败降级。
- 短信模板管理。
- 短信回执处理。

### 24.6 接入网关层统一鉴权

如果后续系统拆分为微服务，可以把一部分能力前移到网关：

- token 基础校验。
- IP 限流。
- 黑名单。
- 统一日志 traceId。
- 请求签名。
- 防重放。

业务服务内部仍保留 Sa-Token 权限注解和细粒度业务校验。

### 24.7 多租户和数据权限

当前商家归属只覆盖了店铺。

后续可以扩展：

- 商家员工。
- 门店管理员。
- 平台运营。
- 区域管理员。
- 多门店品牌方。

数据权限可以从简单的 `own` 扩展成：

```text
own
shop
brand
region
platform
```

这样会更贴近真实本地生活类平台。

## 25. 总结

当前登录鉴权模块的核心变化是：

```text
从“登录态判断”
升级为
“认证 + 授权 + 审计 + 风控 + 后台治理 + 平滑迁移”
```

它现在具备企业项目认证授权模块的基本形态：

- Sa-Token 作为认证授权框架。
- RBAC 作为权限数据模型。
- PermissionService 作为权限查询核心。
- StpInterface 作为框架和业务权限的桥接。
- UserHolder 兼容层保证旧业务平滑迁移。
- CurrentUserService 作为后续统一当前用户入口。
- LoginLog 和 OperationLog 提供审计能力。
- Redis 提供验证码、限流和失败封禁能力。
- Admin 后台接口提供用户、角色、商家归属的基础治理能力。

后续如果继续扩展，建议优先做权限缓存、登录日志后台查询、风控配置化和真实短信服务接入。

## 26. 店铺与分类模块当前定位

当前店铺与分类模块已经从最初的“Controller 直接查库、店铺详情简单缓存、附近店铺按 Redis GEO 查询”升级为具备缓存治理、接口模型隔离、后台分类管理、GEO 索引治理、乐观锁并发保护和审计能力的业务模块。

这次改造的目标不是单纯增加几个接口，而是围绕真实企业项目中店铺类业务常见的问题进行补强：

- 店铺详情读多写少，热点数据需要更稳的缓存击穿保护。
- 分类数据读多写少，需要 Redis 与本地缓存结合，避免每次访问都打到数据库。
- 商家只能维护自己的店铺，不能只依赖权限码，还要有真实归属关系。
- 店铺创建、更新、分类维护、GEO 重建都属于重要后台操作，需要审计。
- 附近店铺查询不能无限使用页码深分页，需要向游标分页收敛。
- 接口不应该直接暴露数据库实体，需要 DTO 和 VO 隔离外部契约。
- 更新店铺需要并发保护，避免多个用户基于旧数据互相覆盖。
- 历史兼容接口在新能力验证后需要下线，避免项目长期背负两套语义。

整体目标是让店铺与分类模块不再只是教学项目中的 CRUD 和简单缓存，而是形成一个可以支持多实例部署、后台治理、商家权限、缓存一致性和接口演进的企业级业务模块。

## 27. 改造前的主要问题

早期店铺与分类模块存在以下典型问题：

- 店铺详情虽然使用 Redis 缓存，但缓存策略比较单一，热点 key 有击穿风险。
- TTL 固定，多个 key 可能同时过期，存在缓存雪崩隐患。
- 互斥锁方案存在递归重试、锁释放不够严谨、线程池不可配置等问题。
- 分类列表每次请求直接查询数据库，没有缓存层。
- 分类查询逻辑在 Controller 中，控制层偏厚。
- 前台直接返回 `ShopType`、`Shop` 等数据库实体，容易暴露数据库字段。
- 店铺创建和更新直接接收实体对象，外部可以提交系统维护字段。
- 店铺更新不检查版本，更新 0 行也可能被认为成功。
- 创建店铺后没有自动绑定商家归属，商家后续无法自然拥有自己的店铺。
- 附近店铺虽然使用 Redis GEO，但缺少参数校验、脏数据处理、业务过滤和深分页治理。
- `/shop/{id}/stats` 和 `/shop/{id}/status` 语义重复。
- `/shop/of/type` 同时承载旧列表响应和新分页能力，接口契约不够统一。

因此，本轮优化不仅修复了单点问题，也对接口模型、缓存策略、后台治理和旧能力下线做了系统整理。

## 28. 新的总体架构思路

当前店铺与分类模块采用如下分层思路：

```text
客户端
  |
  | DTO 请求模型
  v
Controller
  |
  | 参数校验、权限注解、路由分发
  v
Service 业务层
  |
  | 店铺创建、更新、查询、状态统计
  | 分类查询、后台维护
  | 商家归属校验
  | 缓存与 GEO 同步
  v
缓存层
  |
  | Redis 店铺详情缓存
  | Redisson 互斥锁
  | Redis + Caffeine 分类二级缓存
  | Redis 分类版本号
  v
数据层
  |
  | tb_shop
  | tb_shop_type
  | sys_merchant_shop
  | tb_blog
  v
后台治理与审计
  |
  | 分类后台 CRUD
  | GEO 索引重建
  | sys_operation_log
```

这个架构的关键点是：

- Controller 只负责接收参数、权限控制和返回结果。
- Service 负责业务规则，包括权限归属、缓存同步、GEO 同步和审计。
- 外部请求使用 DTO，外部响应使用 VO，数据库实体不再直接暴露。
- 店铺详情缓存使用 `CacheClient` 统一封装。
- 分类列表使用 Redis + Caffeine 二级缓存，并通过 Redis 版本号解决多实例本地缓存失效问题。
- 附近店铺以 Redis GEO 作为距离候选来源，再在候选集合内做业务过滤和排序。
- 后台写操作通过事务提交后的回调触发缓存和 GEO 同步，避免事务未提交时提前污染缓存。
- 旧接口和旧响应在替代能力稳定后逐步下线。

## 29. 店铺详情缓存改造

店铺详情查询当前通过：

```java
cacheClient.queryWithMutex(...)
```

默认走互斥锁方案，而不是只做缓存穿透保护。

这样可以解决几个问题：

- 缓存未命中时只允许少量线程进入数据库查询。
- 热点店铺缓存过期时，避免大量并发请求同时击穿到数据库。
- 查询结果为空时缓存空值，避免不存在店铺被反复查询数据库。
- 正常缓存 TTL 增加随机抖动，降低同一批缓存同时过期的概率。
- 获取锁失败时有限等待和降级，不再使用递归重试。
- 逻辑过期缓存增加物理 TTL 兜底，避免旧数据永久保留。

锁实现使用 Redisson 的 `RLock`，不再依赖简单的 `delete lock key` 释放方式，从而避免极端情况下误删其他线程的锁。

缓存重建线程池也从静态 `Executors.newFixedThreadPool` 改为 Spring 托管的 `ThreadPoolTaskExecutor`，线程池大小、队列容量和优雅关闭时间都可以通过 `hmdp.cache` 配置。

## 30. 店铺接口模型与并发保护

店铺创建和更新不再直接接收 `Shop` 实体，而是拆分为：

```text
ShopCreateDTO
ShopUpdateDTO
ShopDetailVO
NearbyShopVO
ShopStatusVO
```

创建接口只允许提交业务输入字段：

- `name`
- `typeId`
- `images`
- `area`
- `address`
- `x`
- `y`
- `avgPrice`
- `openHours`

系统维护字段不允许外部写入，例如：

- `sold`
- `comments`
- `score`
- `createTime`
- `updateTime`
- `version`

创建店铺时，Service 会设置默认值：

```text
sold = 0
comments = 0
score = 0
version = 0
```

店铺更新增加了乐观锁版本控制：

```text
tb_shop.version
```

`PUT /shop` 当前要求必须提交 `version`。更新时按：

```text
id + version
```

作为条件更新，并执行：

```text
version = version + 1
```

如果影响行数为 0，说明数据已经被其他请求更新，返回 `SHOP_UPDATE_CONFLICT`。这可以避免多个用户基于旧版本数据互相覆盖。

## 31. 商家创建店铺与归属绑定

店铺创建接口支持管理员和商家两类入口权限：

```java
@SaCheckPermission(value = {"shop:create:own", "shop:create"}, mode = SaMode.OR)
```

当前规则是：

- 商家创建店铺后，自动绑定 `merchant_user_id + shop_id`。
- 管理员创建店铺不自动绑定商家。
- 绑定关系写入 `sys_merchant_shop`。
- 后续商家更新店铺时，通过 `MerchantShopService.isShopOwner(...)` 判断归属。

这样 `shop:create:own` 和 `shop:update:own` 不再只是权限码，而是和真实店铺归属数据关联起来。

店铺创建、更新成功后，会在事务提交后执行：

- 写入或刷新 Redis GEO 坐标。
- 删除店铺详情缓存。
- 更新店铺存在性本地缓存。
- 写入操作审计日志。

## 32. 店铺状态统计语义修正

之前店铺状态统计容易把“是否有评价”误当成“店铺是否存在”。

当前已经拆分为明确语义：

- 店铺是否存在：查询 `tb_shop`。
- 评价数量：统计 `tb_blog`。

对外保留的接口为：

```text
GET /shop/{id}/status
```

返回：

```text
exists
reviewCount
```

当店铺不存在时，只返回 `exists=false`，`reviewCount` 不返回。

旧接口：

```text
GET /shop/{id}/stats
```

已经下线，避免两个接口长期表达同一件事。

## 33. 附近店铺查询改造

附近店铺仍然使用 Redis GEO 作为距离查询基础：

```text
shop:geo:{typeId}
```

GEO 中只保存：

- shopId
- 经纬度

查询流程是：

```text
Redis GEO 获取距离候选
  |
  v
按 shopId 回表查询店铺详情
  |
  v
过滤脏 GEO 数据
  |
  v
候选集合内业务过滤和排序
  |
  v
转换为 NearbyShopVO
  |
  v
返回 PageResult
```

当前支持的参数包括：

- `typeId`
- `current`
- `x`
- `y`
- `lastDistance`
- `lastId`
- `sortBy`
- `keyword`
- `area`
- `minScore`
- `minAvgPrice`
- `maxAvgPrice`
- `openNow`
- `pageResult`

参数校验包括：

- `typeId` 必须大于 0。
- `current` 必须大于 0。
- `x/y` 必须成对传入。
- 经度范围为 `[-180, 180]`。
- 纬度范围为 `[-90, 90]`。
- `lastDistance/lastId` 必须成对传入。
- `sortBy` 只支持 `distance`、`score`、`sold`。

GEO 查询半径明确使用 5000 米，避免单位含义不清。

## 34. 附近店铺游标分页与统一响应

旧的 GEO 页码模式会随着页码增大不断扩大 Redis `limit(end)`，深分页性能会逐渐变差。

当前已经下线 GEO 页码分页，改为游标分页：

- 第一页可以只传 `x/y`。
- 第二页开始必须传 `lastDistance + lastId`。
- Service 会跳过上一页游标之前的数据。
- 返回新的游标给下一页使用。

统一响应使用：

```text
PageResult<T>
```

字段包括：

- `list`
- `current`
- `size`
- `total`
- `hasMore`
- `cursor`

附近店铺的游标信息放在：

```text
cursor.lastDistance
cursor.lastId
```

旧的 `NearbyShopResult` 已经删除，`/shop/of/type` 也不再返回裸 `List`。如果显式传 `pageResult=false`，会返回参数错误，保证新接口契约统一。

## 35. 附近店铺业务过滤与排序

当前附近店铺支持在距离候选集合内做轻量业务过滤：

- 关键字过滤：匹配店铺名称和地址。
- 商圈过滤：按 `area` 精确匹配。
- 最低评分：`minScore`。
- 人均价格区间：`minAvgPrice`、`maxAvgPrice`。
- 是否营业中：`openNow`。

营业时间解析支持：

- `HH:mm-HH:mm`
- 多个时间段用逗号分隔
- 跨天营业，例如 `18:00-03:00`
- `00:00-24:00`

排序策略包括：

- `distance`：默认按 Redis GEO 距离顺序。
- `score`：在距离候选集合内按评分倒序，距离作为次排序。
- `sold`：在距离候选集合内按销量倒序，距离作为次排序。

这里刻意没有承诺“全城评分优先”或“全量销量优先”，因为当前没有引入 Elasticsearch 或更复杂的检索系统。现阶段的语义是：

```text
先按距离取候选，再在候选集合内过滤和排序。
```

这样比伪装成全量精确搜索更符合当前技术边界。

## 36. GEO 索引重建与脏数据治理

店铺 GEO 索引支持增量同步和后台重建。

增量同步场景：

- 创建店铺后写入 GEO。
- 更新店铺后刷新 GEO。
- 店铺类型或坐标变化时，从旧分类 GEO 删除并写入新分类 GEO。

后台重建接口：

```text
POST /admin/shops/geo/rebuild
```

支持：

- 不传 `typeId`：重建全部分类 GEO。
- 传 `typeId`：只重建指定分类 GEO。

查询附近店铺时，如果 Redis GEO 中存在脏数据：

- DB 查不到对应 shopId。
- DB 查到但 `typeId` 不匹配。

Service 会从当前 GEO key 中懒删除这些脏成员，避免脏数据长期影响查询。

## 37. 分类模块缓存改造

前台分类列表接口：

```text
GET /shop-type/list
```

现在只返回启用分类，并返回 `ShopTypeVO`：

- `id`
- `name`
- `icon`
- `sort`

不再直接返回数据库实体 `ShopType`。

分类查询逻辑已经下沉到 `ShopTypeServiceImpl`。Controller 只负责调用：

```java
typeService.queryTypeList()
```

分类列表采用 Redis + Caffeine 二级缓存：

```text
Caffeine 本地缓存
  |
  | 未命中或版本变化
  v
Redis 分类列表缓存
  |
  | 未命中
  v
MySQL tb_shop_type
```

Redis key：

```text
cache:shop-type:list
cache:shop-type:version
```

本地缓存命中前会检查 Redis 版本号。如果其他实例更新了分类并递增版本号，当前实例会发现版本变化，主动失效本地缓存并重新加载。

这样既保留本地缓存的高性能，也解决了多实例部署时本地缓存不一致的问题。

## 38. 分类后台管理闭环

新增分类后台接口：

```text
GET    /admin/shop-types
POST   /admin/shop-types
PUT    /admin/shop-types
PATCH  /admin/shop-types/status
DELETE /admin/shop-types/{id}
```

权限要求：

```text
shop:type:manage
```

后台使用的模型包括：

- `ShopTypeCreateDTO`
- `ShopTypeUpdateDTO`
- `ShopTypeStatusDTO`
- `ShopTypeAdminVO`

分类删除采用软删除：

```text
status = 0
```

不做物理删除，避免已有店铺引用分类后出现数据断裂。

分类写操作成功后，会在事务提交后执行：

- 删除 Redis 分类列表缓存。
- 递增 Redis 分类版本号。
- 清理当前实例 Caffeine 本地缓存。
- 写入操作审计日志。

前台分类列表只查询：

```text
status = 1
```

从而保证后台停用的分类不会继续出现在前台。

## 39. 数据库变更

本轮店铺与分类模块新增 migration：

```text
src/main/resources/db/migration/V20260609_01_shop_category_followups.sql
```

主要变更包括：

```sql
ALTER TABLE `tb_shop`
  ADD COLUMN `version` int NOT NULL DEFAULT 0 COMMENT 'Optimistic lock version' AFTER `open_hours`;

ALTER TABLE `tb_shop_type`
  ADD COLUMN `status` tinyint(1) NOT NULL DEFAULT 1 COMMENT '1 enabled, 0 disabled' AFTER `sort`,
  ADD KEY `idx_tb_shop_type_status_sort` (`status`, `sort`) USING BTREE;
```

同时新增权限：

- `shop:create:own`
- `shop:type:manage`
- `shop:geo:rebuild`

并更新初始化脚本：

```text
src/main/resources/db/hmdp.sql
```

保证新环境初始化时包含版本字段、分类状态字段、索引和权限数据。

## 40. 旧接口和旧能力下线

在替代功能完成并通过测试后，当前已经下线了一批旧能力：

- `/shop/{id}/stats` 下线，统一使用 `/shop/{id}/status`。
- `PUT /shop` 不再兼容缺少 `version` 的更新请求。
- `/shop/of/type` 不再返回裸 `List`，统一返回 `PageResult`。
- `/shop/of/type` 不再保留 GEO 页码深分页模式，统一使用游标分页。
- 删除 `NearbyShopResult`，游标信息统一放入 `PageResult.cursor`。
- 删除内部 7 参数版 `queryShopByType(...)` 兼容重载。
- 删除旧的 `updateShopWithOptionalVersion(...)` 分支。
- 删除无触发路径的 `SHOP_UPDATE_FAILED` 错误码。

这部分精简的价值是减少长期维护两套接口语义，避免前端、测试和后端服务层同时存在新旧两种契约。

## 41. 测试覆盖

本轮店铺与分类模块补充了多类测试。

缓存层测试：

```text
CacheClientTest
```

覆盖：

- TTL 抖动。
- 互斥锁命中、重建、锁超时降级。
- 逻辑过期物理 TTL。

分类服务测试：

```text
ShopTypeServiceImplTest
```

覆盖：

- 本地缓存命中。
- Redis 命中回填本地缓存。
- Redis 未命中查 DB 并写缓存。
- Redis 异常兜底 DB。
- Redis 版本变化触发本地缓存失效。
- 分类新增、状态更新、软删除后的缓存失效和版本递增。

店铺领域测试：

```text
ShopServiceImplDomainModelTest
```

覆盖：

- 创建店铺默认字段。
- 商家创建店铺自动绑定。
- 管理员创建店铺不自动绑定。
- 店铺不存在。
- 商家无归属权限。
- 乐观锁版本冲突。
- 更新成功后删除缓存、刷新 GEO、更新存在性缓存。

附近店铺测试：

```text
ShopServiceImplNearbyTest
```

覆盖：

- 参数非法返回参数错误。
- GEO 使用 5000 米距离单位。
- 脏 GEO 数据过滤和懒删除。
- 游标分页跳过上一页数据。
- 评分和销量候选集内排序。
- 业务过滤参数。
- `PageResult` 统一响应。
- 旧裸列表响应和旧 GEO 页码模式被拒绝。

Controller 测试：

```text
ShopControllerValidationTest
AdminShopControllerTest
```

覆盖：

- 店铺创建和更新参数校验。
- 更新店铺缺少 `version` 返回参数错误。
- `/shop/{id}/status` 正常访问。
- `/shop/{id}/stats` 已下线。
- 分类后台接口参数校验、响应 VO 和审计。
- GEO 重建失败返回领域错误码。

验证命令：

```bash
mvn -q "-Dtest=CacheClientTest,ShopTypeServiceImplTest,ShopServiceImplNearbyTest,ShopServiceImplDomainModelTest,ShopControllerValidationTest,ShopStatsServiceTest,ShopGeoIndexServiceTest,AdminShopControllerTest" test
mvn -q -DskipTests test
```

## 42. 当前完成度判断

从店铺与分类模块角度看，当前已经完成了比较完整的一阶段企业级改造。

已经具备：

- 店铺详情热点缓存保护。
- 缓存 TTL 随机抖动。
- Redisson 互斥锁。
- Spring 托管缓存重建线程池。
- DTO/VO 接口模型隔离。
- JSR-303 参数校验。
- 店铺乐观锁版本控制。
- 商家创建店铺自动绑定归属。
- 商家更新店铺归属校验。
- 店铺状态统计语义修正。
- Redis GEO 附近店铺查询。
- GEO 脏数据懒清理。
- GEO 索引后台重建。
- 附近店铺游标分页。
- 统一 `PageResult` 响应。
- 附近店铺候选集内过滤和排序。
- 分类 Redis + Caffeine 二级缓存。
- 分类 Redis 版本号跨实例失效。
- 分类后台 CRUD 和软删除。
- 分类写操作缓存刷新闭环。
- 操作审计。
- 领域错误码。
- 旧接口和旧兼容分支精简。

因此，这部分已经不只是“能查店铺、能查分类”，而是形成了一个具备缓存治理、并发控制、后台管理、权限归属、数据一致性和接口演进能力的业务子系统。

## 43. 后续可继续优化方向

当前店铺与分类模块已经形成闭环，但如果继续贴近更复杂的企业项目，还可以进一步增强。

### 43.1 搜索能力升级

当前附近店铺过滤是在 Redis GEO 距离候选集合内完成的。

如果后续要支持更复杂的搜索，例如：

- 全城关键字搜索。
- 多条件组合过滤。
- 评分、销量、距离综合排序。
- 拼音、分词、纠错。

可以引入 Elasticsearch 或 OpenSearch，将 Redis GEO 继续作为附近场景的距离索引，搜索引擎负责复杂检索。

### 43.2 店铺状态字段

当前没有扩展店铺营业状态、审核状态、上下架状态等字段。

后续可以增加：

- `status`
- `audit_status`
- `business_status`

并在附近店铺、店铺详情和后台管理中统一过滤。

### 43.3 分类缓存失效通知增强

当前使用 Redis 版本号解决多实例本地缓存失效。

后续如果分类写操作更频繁，可以考虑：

- Redis Pub/Sub。
- MQ 广播缓存失效事件。
- Canal 监听数据库变更。

### 43.4 店铺操作审计详情增强

当前审计记录了关键操作和结果。

后续可以记录更细的字段差异：

- 修改前值。
- 修改后值。
- 操作来源。
- 请求 traceId。

这样更适合后台运营追责和问题回溯。

### 43.5 管理后台能力继续补齐

当前已经有分类后台管理和 GEO 重建。

后续可以继续补：

- 店铺后台分页管理。
- 店铺审核。
- 店铺上下架。
- 店铺归属转移。
- 批量重建 GEO。
- 分类排序拖拽保存。

这会让店铺与分类模块从“业务接口完善”进一步升级为“后台运营闭环完善”。

## 44. 探店笔记模块企业化改造记录

本轮探店笔记模块主要围绕 `BlogController`、`BlogServiceImpl`、点赞体系、热门笔记、关注流、图片上传和店铺摘要旁路查询进行优化。
目标不是简单让接口可用，而是把课堂演示型实现改成更接近企业项目的结构：MySQL 作为事实源，Redis 作为查询加速层，接口层使用 DTO/VO 隔离数据库实体，异步事件负责非核心链路扩散。

### 44.1 已落地的核心改动

发布笔记接口从直接接收 `Blog` 实体改成接收 `BlogCreateRequest`。
请求字段限定为：

```text
shopId
title
images
content
```

并通过 JSR-303 做基础校验，避免前端直接透传数据库字段，例如 `liked`、`comments`、`status`、`deleted` 等。
列表和详情返回也改为 `BlogListItemVO`、`BlogDetailVO`，不再把数据库实体直接暴露给前端。

点赞体系改成 MySQL + Redis 双层模型：

- `tb_blog_like` 保存用户点赞事实数据。
- `tb_blog.liked` 保存计数字段。
- Redis `blog:liked:{blogId}` ZSet 保存点赞用户和时间戳，用于是否点赞和点赞榜查询加速。
- 点赞和取消点赞在事务内更新点赞表和 `tb_blog.liked`。
- Redis 更新通过事件异步执行，Redis 失败不影响主流程。
- 提供一致性修复能力，可以从 MySQL 重建单篇或全量点赞 ZSet，并校准 `liked` 计数。

热门笔记从单纯 MySQL 排序升级为 Redis 热榜优先：

- `GET /blog/hot` 优先读取 Redis `blog:hot` ZSet。
- Redis 未命中或存在脏 ID 时回退 MySQL。
- MySQL 回退后回填热门缓存。
- 点赞变化后异步更新热门分值。
- 当前热度分值 v1 使用 `liked`，后续可以扩展为点赞数、发布时间、评论数、收藏数综合得分。

关注流改成推拉结合：

- 普通作者发布笔记后，通过 `BlogPublishedEvent` 异步写入粉丝 `feed:{userId}` ZSet 收件箱。
- ZSet score 使用发布时间戳，支持滚动分页。
- 每个收件箱保留最近 `1000` 条，避免 Redis 长期膨胀。
- 大粉作者达到阈值后不再全量推送，用户刷关注流时通过拉模式读取这些作者最近笔记，再与收件箱结果合并排序。
- 关注流分页大小抽为配置项，避免硬编码。
- 相同时间戳下通过 `lastId + offset` 处理滚动分页，减少重复和漏数据。

查询性能方面，列表页和详情页批量查询用户轻量信息，并增加 `user:brief:{userId}` 缓存，避免循环 `getById` 造成 N+1 查询。

内容状态方面，`tb_blog` 增加了：

```text
status
deleted
publish_time
```

热门、详情、关注流、指定用户笔记、店铺摘要和评论数统计都统一只查询已发布且未删除的笔记。

### 44.2 本轮继续补齐的边界加固

`Blog` 实体已经去掉 `icon`、`name`、`isLike` 这类展示字段。
这些字段现在由 VO 承接，实体只表达数据库表结构，降低实体被接口层污染的风险。

图片上传接口做了安全加固：

- 上传文件必须非空。
- 单张图片限制为 5MB。
- 后缀只允许 `jpg`、`jpeg`、`png`、`webp`。
- 保存路径使用规范化后的 canonical path，防止 `../` 目录穿越。
- 删除图片时只允许删除 `blogs/` 目录下的文件。
- 删除接口从旧的 `GET /upload/blog/delete` 改为 `DELETE /upload/blog`。

旧的 `GET /upload/blog/delete` 已经下线，因为删除资源不应该使用 GET 语义。
当前删除接口兼容传入 `/blogs/...` 和历史 `/imgs/blogs/...` 两类路径，减少前端或历史数据迁移成本。

Blog 查询和互动入口增加了基础参数校验：

- `blogId` 为空或小于等于 0 时直接返回参数错误。
- 点赞榜查询会先判断笔记是否存在且处于可见状态。
- 关注流没有数据时统一返回空 `ScrollResult`，而不是空响应体。
- Redis feed 中出现异常 member 时只跳过脏数据，不让整页关注流失败。

店铺摘要、AI 总结、店铺评价数等旁路查询也统一增加：

```sql
status = 1 AND deleted = 0
```

这样可以避免已删除或未发布笔记继续进入摘要、推荐、统计口径。

### 44.3 数据库和索引优化

点赞表使用唯一索引保障幂等：

```sql
uk_blog_user(blog_id, user_id)
```

这样即使前端重复点击、网络重试或并发请求同时到达，也不会生成重复点赞记录。

热门列表建议使用组合索引：

```sql
idx_blog_liked_time(liked, create_time)
```

用于 Redis 热榜失效后的 MySQL 回退查询。

关注关系表建议增加：

```sql
uk_follow_user(user_id, follow_user_id)
idx_followed_user(follow_user_id)
```

前者保证关注关系幂等，后者提升发布笔记后查询粉丝列表的效率。

### 44.4 测试覆盖

本轮默认测试已通过：

```bash
mvn -q -DskipTests compile
mvn -q clean test
```

新增或已有测试覆盖的重点包括：

- 重复点赞只生成一条点赞记录。
- 取消未点赞不会让 `liked` 变成负数。
- Redis 点赞 ZSet 缺失时可以回退 MySQL。
- 热门榜 Redis 未命中时可以回退 MySQL 并回填。
- 发布事务回滚时不会写入粉丝 feed。
- 关注流滚动分页在相同时间戳下不丢数据、不重复数据。
- 大粉作者内容通过拉模式进入关注流。
- 店铺评价数量只统计已发布且未删除的笔记。

### 44.5 优化思路总结

这次探店笔记模块的核心思路可以概括为四点。

第一，事实源和缓存分层。
点赞、发布、内容状态这类关键数据以 MySQL 为准，Redis 只承担加速查询和降低读压力的角色。
这样 Redis 丢失、过期或出现脏数据时，可以通过 MySQL 修复，而不会丢业务事实。

第二，主链路和扩散链路分离。
发布笔记、点赞这类用户主操作只保证核心数据落库；热门分值、点赞 ZSet、粉丝 feed 等扩散动作通过事件异步处理。
这样可以降低接口响应耗时，也避免 Redis 写入失败导致主业务失败。

第三，接口模型和数据库模型隔离。
请求使用 DTO，响应使用 VO，实体只做持久化模型。
这样后续新增审核字段、风控字段、推荐字段时，不会直接暴露给前端，也不会让前端请求污染数据库字段。

第四，统一数据可见性口径。
只要是面向用户或 AI 摘要的查询，都必须过滤已删除和未发布内容。
这类规则不能只写在主接口里，也要覆盖旁路统计、摘要、推荐、缓存回源等链路。

### 44.6 后续还可以继续升级的方向

评论模块目前仍是空脚手架，暂时没有删除。
如果后续继续企业化，可以把评论单独设计为完整子模块：

- `tb_blog_comment`
- 评论发布、删除、审核、置顶。
- 评论数异步聚合。
- 热门评论缓存。
- 评论敏感词和风控。

内容治理可以继续增强：

- 草稿箱。
- 审核中、审核拒绝、已发布、已下线等状态流转。
- 管理端上下线接口。
- 操作审计。
- 删除原因和下线原因。

图片能力可以继续升级：

- 从本地 nginx 目录迁移到对象存储。
- 增加图片元数据表。
- 支持多图排序、封面图、图片宽高和大小。
- 增加 CDN 域名、缩略图、水印和违规图片扫描。

推荐和热榜可以继续升级：

- 热榜分值从单一 `liked` 扩展为时间衰减模型。
- 结合评论数、收藏数、点击数、完读率。
- 引入用户兴趣标签和店铺分类标签。
- 对新内容增加冷启动扶持。

关注流可以继续升级：

- 大粉作者阈值动态配置。
- Feed 收件箱按活跃用户分层保留不同长度。
- 引入 MQ 或 Outbox Pattern，提高异步扩散可靠性。
- 增加 feed 修复任务，按关注关系和发布时间重建用户收件箱。

风控和限流可以继续升级：

- 发布笔记频率限制。
- 点赞频率限制。
- 重复内容检测。
- 敏感词过滤。
- 黑名单用户限制互动。
- IP、设备、账号多维度风控。

这部分如果继续推进，探店笔记模块可以从“内容发布 + 点赞 + 关注流”进一步升级为具备内容治理、互动风控、推荐排序、异步补偿和后台运营能力的内容子系统。

## 45. 社交关注模块企业化改造记录

本轮社交关注模块主要围绕 `FollowController`、`FollowServiceImpl`、`FollowMapper`、关注关系缓存、共同关注查询、关注流脏数据治理和后台修复能力进行改造。目标不是单纯让关注、取关接口可用，而是把原本偏课堂示例的实现升级为更接近企业项目的结构：MySQL 作为关注关系事实源，Redis Set 作为查询加速层，接口语义 REST 化，写入幂等化，缓存可重建，脏数据可修复，异常场景可降级。

### 45.1 改造前的主要问题

早期关注模块的实现比较直接：

```text
PUT /follow/{id}/{isFollow}
GET /follow/or/not/{id}
GET /follow/common/{id}
```

关注和取关通过一个布尔参数区分，接口语义不够清晰；查询是否关注和共同关注接口也带有较强的旧式路径风格。

数据层面主要存在以下问题：

- 关注关系写入依赖普通 `save`，重复关注在并发或重试场景下容易产生重复数据，或者需要靠异常兜底。
- Redis Set 保存关注列表，但缺少明确的冷缓存重建和后台修复入口。
- 共同关注直接返回完整交集列表，用户关注量较大时容易一次性返回过多数据。
- Redis 出现异常或脏 member 时，缺少统一的降级和容错策略。
- 取关后，用户关注流 `feed:{userId}` 中可能残留已取关作者的历史动态。
- 缺少面向运维和管理员的缓存重建、feed 修复接口。
- 旧接口长期保留会导致新旧契约并存，增加前后端、测试和维护成本。

因此，本轮优化重点是让关注模块形成“写入可靠、读取高效、缓存可恢复、接口语义清晰、后台可治理”的闭环。

### 45.2 新接口设计

旧接口已经删除，当前关注模块使用更清晰的资源语义：

```text
PUT    /follow/{id}
DELETE /follow/{id}
GET    /follow/{id}/status
GET    /follow/{id}/common?current=1&size=20
```

含义分别是：

- `PUT /follow/{id}`：关注指定用户。
- `DELETE /follow/{id}`：取关指定用户。
- `GET /follow/{id}/status`：查询当前登录用户是否关注了指定用户。
- `GET /follow/{id}/common`：分页查询当前用户与指定用户的共同关注。

共同关注接口返回统一的 `PageResult<UserDTO>`，而不是一次性返回完整列表：

```text
list
current
size
total
hasMore
cursor
```

当前采用普通页码分页，`size` 限制为 `1..100`。这样可以避免共同关注数量较大时，接口一次性返回过多用户信息。

### 45.3 关注关系写入幂等化

关注关系仍然以 MySQL `tb_follow` 作为事实源，并通过唯一索引保证同一个用户不能重复关注同一个对象：

```sql
uk_follow_user(user_id, follow_user_id)
```

写入层新增 `FollowMapper.insertIgnore(...)`：

```java
@Insert("INSERT IGNORE INTO tb_follow(user_id, follow_user_id) VALUES(#{userId}, #{followUserId})")
int insertIgnore(@Param("userId") Long userId, @Param("followUserId") Long followUserId);
```

这样重复关注不再依赖 `DuplicateKeyException` 控制正常业务流程，而是由数据库唯一索引和 `INSERT IGNORE` 共同保证幂等：

- 第一次关注：插入成功，返回 `1`。
- 重复关注：不插入新记录，返回 `0`，业务仍视为成功。
- 并发关注：最终只有一条关系记录落库。

这类写法更适合真实项目中的网络重试、按钮重复点击、并发请求等场景。

### 45.4 Redis 关注缓存与 MySQL 回退

当前关注列表缓存使用 Redis Set：

```text
follows:{userId}
follows:loaded:{userId}
```

其中：

- `follows:{userId}` 保存用户关注的用户 ID 集合。
- `follows:loaded:{userId}` 表示该用户关注缓存已经从 MySQL 加载过，即使关注列表为空也可以标记，避免缓存穿透式反复查库。

查询是否关注时，流程是：

```text
先判断 follows:loaded:{userId}
  如果已加载：读 Redis Set 判断 isMember
  如果未加载：从 MySQL 查询关注列表，重建 Redis Set，再读 Redis
Redis 异常时：回退 MySQL 查询
```

这保证了两个目标：

- 正常情况下走 Redis，查询快。
- Redis 缺失、过期、异常时，MySQL 仍然可以兜底，业务不依赖缓存作为事实源。

关注和取关成功后，Redis 同步放到事务提交之后执行：

```text
MySQL 事务提交成功
  -> 同步关注缓存
  -> 标记 loaded key
```

这样避免数据库事务回滚但 Redis 已经提前变更的问题。

### 45.5 共同关注分页与降级

共同关注优先使用 Redis Set 交集：

```text
SINTER follows:{currentUserId} follows:{targetUserId}
```

然后对交集 ID 做：

```text
解析 Long
过滤异常 member
去重
排序
内存分页
批量查询 UserDTO
包装 PageResult
```

如果 Redis 查询失败，服务会回退到 MySQL：

```text
查询当前用户关注列表
查询目标用户关注列表
在服务内求交集
排序分页
批量查询用户信息
```

当前做法的关键点是两条路径的返回语义一致：无论 Redis 命中还是 MySQL 回退，调用方拿到的都是 `PageResult<UserDTO>`。

### 45.6 取关后的 feed 清理

关注流使用 Redis ZSet：

```text
feed:{userId}
```

当用户取关某个作者后，如果不处理 feed，用户的关注流里可能还会看到该作者之前推送进收件箱的内容。

当前取关成功后会执行清理：

```text
查询被取关作者最近的已发布、未删除笔记
从当前用户 feed:{userId} ZSet 中移除这些 blogId
```

这属于主动修正，目的是让取关行为尽快反映到关注流里。

同时，关注流查询侧也增加了防御：读取 feed 候选内容时，会结合当前关注关系过滤已不再关注的作者，避免 Redis 中存在历史脏数据时直接透出给用户。

### 45.7 后台一致性修复能力

新增 `FollowConsistencyService`，把运维修复能力从普通用户关注服务中拆出来，避免 `IFollowService` 变成既处理用户业务又处理后台治理的混合接口。

当前提供三类能力：

```java
Map<String, Object> rebuildFollowCache(Long userId);

Map<String, Object> rebuildAllFollowCaches();

Map<String, Object> repairUserFeed(Long userId);
```

含义分别是：

- 重建单个用户的关注 Redis Set。
- 基于 `tb_follow` 分组重建所有有关注关系用户的 Redis Set。
- 修复单个用户 feed，移除不属于当前关注作者、已下线或不存在的 blogId。

后台接口为：

```text
POST /admin/follows/cache/rebuild?userId=
POST /admin/follows/feed/repair?userId=
```

其中：

- `cache/rebuild` 的 `userId` 可选；不传时执行全量重建。
- `feed/repair` 必须传 `userId`，修复指定用户关注流。
- 两个接口都需要 `follow:repair` 权限。
- 操作会写入 `sys_operation_log`，便于后续审计。

这类后台修复入口的价值是：当 Redis 数据丢失、缓存过期策略调整、线上出现脏 feed、历史数据迁移后缓存不一致时，不需要手工连 Redis 处理，可以通过受权限保护的管理端接口进行修复。

### 45.8 权限和数据库脚本

关注模块补充了后台修复权限：

```text
follow:repair
```

新增迁移脚本：

```text
V20260610_02__follow_repair_permission.sql
```

同时更新初始化脚本：

```text
src/main/resources/db/hmdp.sql
```

这样 Flyway 迁移环境和新库初始化环境都能拿到一致的权限数据。

关注关系表层面的关键索引为：

```sql
uk_follow_user(user_id, follow_user_id)
idx_followed_user(follow_user_id)
```

前者保证关注关系幂等，后者用于提升“查询某个作者的粉丝列表”这类场景的效率，例如发布笔记后向粉丝 feed 扩散。

### 45.9 测试覆盖

本轮补充和调整了以下测试：

```text
FollowControllerTest
FollowServiceImplTest
FollowConsistencyServiceTest
AdminFollowControllerTest
```

覆盖重点包括：

- 新 REST 关注接口可用。
- 旧关注接口已下线。
- 关注写入使用 `insertIgnore` 幂等成功。
- 重复关注不会导致业务失败。
- 非法关注目标、自己关注自己会返回参数错误。
- 查询是否关注支持 Redis 冷缓存重建和 MySQL 回退。
- 共同关注支持分页、非法分页参数校验、Redis 异常回退 MySQL。
- 取关后清理 feed。
- 关注缓存单用户和全量重建。
- 用户 feed 修复会清理无效 blogId 和非当前关注作者的 blogId。
- 管理端接口会记录操作日志。

验证命令：

```bash
mvn -q "-Dtest=FollowControllerTest,FollowServiceImplTest,FollowConsistencyServiceTest,AdminFollowControllerTest" test
mvn -q test
```

### 45.10 核心优化思路总结

这次社交关注模块的核心思路可以总结为五点。

第一，MySQL 是事实源，Redis 是加速层。关注关系的最终正确性由 `tb_follow` 和唯一索引保证，Redis 只负责提升查询性能。Redis 丢失、过期或出现脏数据时，都可以从 MySQL 重建。

第二，写入链路要幂等。关注这种用户高频操作很容易出现重复点击、网络重试和并发请求，使用 `INSERT IGNORE + 唯一索引` 可以把重复请求自然收敛成一次有效写入。

第三，接口契约要清晰。用 `PUT /follow/{id}` 和 `DELETE /follow/{id}` 替代 `/{id}/{isFollow}`，能减少布尔参数语义歧义；旧接口删除后，也避免长期维护两套契约。

第四，读接口要有降级路径。共同关注和是否关注都优先读 Redis，但 Redis 异常时不能让核心业务直接失败，应回退 MySQL，保证可用性。

第五，后台要有修复闭环。企业项目不能只依赖“缓存自然过期”解决一致性问题，还需要提供受权限控制、可审计的修复入口，让线上数据治理有明确工具。

### 45.11 后续还可以继续增强的方向

当前关注模块已经完成一版较完整的后端优化闭环。如果继续向更大规模生产场景推进，可以考虑：

- 全量重建关注缓存改成分批扫描，避免一次性加载全部 `tb_follow`。
- feed 修复改成分页扫描 ZSet，避免超大收件箱一次性读全量。
- 管理端修复任务异步化，返回任务 ID，后台执行并记录进度。
- 关注、取关增加频率限制，避免恶意刷关注关系。
- 引入 MQ 或 Outbox Pattern 处理 feed 扩散和取关清理，提高异步链路可靠性。
- 关注关系增加审计或事件表，便于追踪历史关注/取关行为。
- 对大 V 用户继续优化推拉结合策略，避免粉丝量过大时写扩散压力集中。

因此，当前模块已经不只是“能关注、能取关、能查共同关注”，而是形成了关注关系事实源、Redis 查询加速、接口语义演进、缓存一致性修复、关注流脏数据治理和后台运维入口的一套完整闭环。

## 46. 优惠券与秒杀订单模块企业化改造记录

本轮优惠券与秒杀订单模块主要围绕 `VoucherController`、`VoucherOrderController`、`VoucherServiceImpl`、`VoucherOrderServiceImpl`、`SeckillVoucherServiceImpl`、`seckill.lua` 和数据库迁移脚本进行优化。

这次改造的目标不是简单把“秒杀下单”接口跑通，而是把原本偏课堂示例的秒杀链路升级为更接近企业项目的形态：Redis 承接高并发入口，Lua 保证库存扣减和一人一单的原子性，Redis Stream 承担削峰后的异步落库，MySQL 作为最终事实源和一致性兜底，Redisson 延迟队列处理超时未支付关闭，定时补偿任务处理异常恢复。

当前模块形成的核心链路可以概括为：

```text
商家新增秒杀券
  |
  | 写入 tb_voucher / tb_seckill_voucher
  | 预热 Redis 库存和活动时间
  v
用户发起秒杀
  |
  | seckill.lua 原子校验活动、库存、一人一单
  | 扣 Redis 库存
  | 写入 stream.orders
  v
异步订单消费者
  |
  | 消费 Redis Stream
  | MySQL 落库
  | 扣 tb_seckill_voucher.stock
  | ACK 消息
  v
订单生命周期
  |
  | 支付成功：status=1 -> 2
  | 超时关闭：status=1 -> 4
  | 两者通过条件更新互斥
  v
补偿治理
  |
  | pending 消息重试
  | 死信 stream.orders.dead
  | Redisson 延迟队列
  | 定时扫表关闭超时未支付订单
  | 启动预热 Redis 秒杀活动 key
```

### 46.1 改造前的主要问题

早期优惠券秒杀模块已经具备基本的 Controller、Service 和 Redis Stream 消费框架，但整体还停留在“能演示”的阶段。

主要问题包括：

- `seckillVoucher()` 入口仍是占位式成功返回，没有真正接入 `seckill.lua`。
- Redis 库存扣减、一人一单判断、写 Stream 还没有形成 Java 入口到 Lua 的完整闭环。
- Redis Stream 消费者虽然有初始化、pending、ACK 和异步落库雏形，但批量消费、死信、消费者唯一名、多实例可靠性还不完整。
- MySQL 层没有唯一约束兜底，一人一单只依赖 Redis Set，Redis 数据异常时缺少最终约束。
- 异步落库时没有同步扣减 `tb_seckill_voucher.stock`，MySQL 不能作为后台管理和对账事实源。
- 订单状态字段存在，但缺少支付成功、超时未支付关闭等生命周期动作。
- 秒杀活动开始时间和结束时间只存在数据库中，Lua 层无法在高并发入口直接拒绝未开始或已结束活动。
- Redis 秒杀库存依赖新增券时写入，历史数据或 Redis 丢失后缺少启动预热兜底。
- 超时未支付取消如果只依赖内存任务或 key 过期通知，会存在重启丢失、事件不可靠的问题。

因此，本轮优化重点是把秒杀模块从“单接口成功返回”升级为“高并发入口原子化、异步链路可靠化、订单状态闭环化、库存事实源明确化、异常场景可补偿”的结构。

### 46.2 秒杀入口接入 Lua 原子主链路

当前 `seckillVoucher()` 已经改成真正执行 `seckill.lua`：

```text
生成订单 ID
获取当前用户 ID
执行 Lua 脚本
根据返回码映射业务结果
成功时直接返回 orderId
```

Lua 返回码约定为：

```text
0 成功
1 库存不足
2 重复下单
3 活动未准备好或 Redis key 不完整
4 活动尚未开始
5 活动已结束
```

这样接口不再返回固定的“秒杀成功”字符串，而是返回真正的订单 ID。前端或后续支付链路可以基于该订单 ID 查询订单状态、发起支付或展示排队结果。

Lua 脚本内部把以下动作放在同一个 Redis 原子过程里：

```text
校验库存 key、活动开始 key、活动结束 key 是否存在
校验 Redis 服务端时间是否处于秒杀窗口
判断库存是否大于 0
判断用户是否已经下单
扣减 Redis 库存
记录用户已下单
写入 stream.orders
```

这条链路的关键点是：高并发下最容易出错的库存扣减和一人一单判断不交给 Java 多次网络请求处理，而是交给 Redis Lua 原子执行，避免并发穿透到 MySQL。

### 46.3 Redis 秒杀活动预热和时间窗口校验

新增秒杀券时，当前会同时写入三类 Redis key：

```text
seckill:stock:{voucherId}
seckill:begin:{voucherId}
seckill:end:{voucherId}
```

其中：

- `seckill:stock:{voucherId}` 用于 Lua 原子扣减库存。
- `seckill:begin:{voucherId}` 用于判断活动是否开始。
- `seckill:end:{voucherId}` 用于判断活动是否结束。

Lua 使用 Redis 服务端时间：

```lua
redis.call('time')
```

这样可以避免应用服务器时间不一致造成活动窗口判断漂移。对于秒杀场景，时间判断越靠近库存扣减点越可靠，因此把活动时间校验放入 Lua，比在 Java 层提前查库判断更适合高并发入口。

同时，为了兼容历史数据和 Redis 冷启动，`SeckillVoucherServiceImpl` 增加了启动预热逻辑：

```text
查询 tb_seckill_voucher
为每个秒杀券补齐 stock/begin/end Redis key
stock 使用 setIfAbsent，不覆盖 Redis 中正在变化的实时库存
begin/end 直接刷新，保证 Lua 时间窗口 key 存在
```

这里的关键取舍是：Redis 中的库存可能正在被秒杀请求实时扣减，启动预热不能简单用数据库库存覆盖 Redis 库存，否则多实例重启时可能把已经扣掉的库存重新加回来。因此 stock 采用 `setIfAbsent`，只在 key 缺失时从 MySQL 恢复。

### 46.4 Redis Stream 异步订单处理可靠性增强

秒杀入口成功后不会同步写 MySQL，而是写入 `stream.orders`，由后台消费者异步处理。

当前消费者的核心策略包括：

```text
消费者组：g1
Stream：stream.orders
批量消费：count 50
消费者名：hostname + UUID 短串
成功落库后 ACK
失败不 ACK，保留 pending
pending 超时后可被当前消费者 claim
超过最大投递次数后写入 stream.orders.dead
```

相比简单的单条读取和固定消费者名，当前做法解决了几个生产化问题：

- 多实例部署时不会所有实例都使用同一个 `c1` 消费者名。
- 消费失败不会被吞掉，消息会留在 pending 中等待重试。
- 旧实例崩溃留下的 pending 消息可以被其他实例抢回处理。
- 反复失败的消息不会无限重试阻塞主队列，而是进入死信 Stream。
- ACK 只发生在 DB 落库成功之后，避免“消息已确认但订单没入库”。

这里的核心思路是：Redis Stream 按 at-least-once 语义处理，业务层必须做好幂等。消息重复投递并不可怕，可怕的是没有数据库唯一约束和状态条件更新。

### 46.5 MySQL 作为最终一致事实源

Redis 负责高并发入口和削峰，MySQL 负责最终事实。

当前 MySQL 层补充了以下能力：

```text
tb_voucher_order 增加唯一索引 uk_voucher_order_user_voucher(user_id, voucher_id)
tb_voucher_order 增加 idx_voucher_order_voucher(voucher_id, create_time)
tb_voucher_order 增加 idx_voucher_order_status_create(status, create_time)
```

唯一索引用于兜底一人一单。即使 Redis Set 异常、消息重复投递、消费者重试，数据库也不会插入同一用户同一优惠券的重复订单。

异步落库时使用事务处理：

```text
插入 tb_voucher_order
扣减 tb_seckill_voucher.stock
事务提交后投递订单超时关闭任务
```

其中 `tb_seckill_voucher.stock` 的扣减条件为：

```sql
stock = stock - 1
WHERE voucher_id = ?
  AND stock > 0
```

这让 MySQL 也保留一份真实库存，用于后台管理、对账、补偿和系统恢复。Redis 可以承担秒杀瞬时流量，但不能成为唯一事实源。

### 46.6 超时未支付关闭和库存回补

订单创建后默认处于：

```text
status = 1 未支付
```

订单落库事务提交后，会向 Redisson `RDelayedQueue` 投递订单关闭任务，默认 15 分钟后触发。

到期关闭逻辑为：

```text
查询订单
只有 status=1 未支付才允许关闭
条件更新 status=1 -> status=4
回补 tb_seckill_voucher.stock
事务提交后回补 Redis stock 和 seckill:order 用户标记
```

Redis 回补包括：

```text
INCR seckill:stock:{voucherId}
SREM seckill:order:{voucherId} userId
```

这样用户未支付被取消后，库存回到 Redis 和 MySQL，用户也可以再次参与该券秒杀。

当前没有只依赖 Redisson 延迟队列，而是增加了定时扫表兜底：

```text
每分钟扫描 status=1 且 create_time 超过支付超时时间的订单
逐笔调用 closeUnpaidVoucherOrder
```

这样即使服务在投递延迟任务后重启、延迟队列消费异常、Redisson 短暂不可用，MySQL 中的未支付订单仍能被后续补偿任务关闭。

### 46.7 支付成功状态机

为了让超时取消不是孤立动作，本轮补充了支付成功接口：

```text
POST /voucher-order/pay/{id}
```

权限码为：

```text
voucher:pay
```

支付逻辑只允许订单从：

```text
status=1 未支付
```

更新为：

```text
status=2 已支付
```

并写入 `pay_time`。

超时关闭则只允许：

```text
status=1 未支付
```

更新为：

```text
status=4 已取消
```

因此支付成功和超时关闭之间不需要复杂分布式锁，而是依赖数据库条件更新自然互斥：

```text
谁先把 status=1 更新掉，谁成功
后到的一方发现状态已变化，直接跳过或返回失败
```

这是一种比“先查状态再业务判断”更可靠的状态机写法，适合订单、支付、退款、核销等强状态流转场景。

### 46.8 权限和数据库脚本

本轮优惠券秒杀模块新增和调整了以下迁移脚本：

```text
V20260610_03__voucher_seckill_enterprise_upgrade.sql
V20260610_04__voucher_order_timeout_close_index.sql
V20260610_05__voucher_pay_permission.sql
```

主要内容包括：

- 清理历史重复订单。
- 增加 `(user_id, voucher_id)` 唯一约束。
- 增加按券和下单时间查询的普通索引。
- 增加按订单状态和创建时间扫描的索引。
- 新增 `voucher:pay` 权限并绑定给 `buyer` 角色。

权限层面的关键变化是：秒杀下单和订单支付拆成两个权限码。

```text
voucher:seckill
voucher:pay
```

当前项目体量下，两者都绑定给买家即可；但拆开权限码后，后续如果做风控、黑名单、支付能力灰度或后台代支付审核，会有更清晰的权限边界。

### 46.9 配置化能力

超时关闭相关参数已经从硬编码中抽出：

```yaml
hmdp:
  voucher:
    order:
      pay-timeout-minutes: 15
      close-scan-limit: 100
      close-scan-fixed-delay-millis: 60000
      close-scan-initial-delay-millis: 60000
```

当前配置的含义是：

- 订单 15 分钟未支付自动关闭。
- 每次补偿扫描最多处理 100 条超时未支付订单。
- 补偿任务每 60 秒执行一次。
- 应用启动 60 秒后开始第一次补偿扫描。

配置化的价值是让本地测试、演示环境和生产环境可以使用不同策略。例如本地可以把支付超时时间调成 1 分钟，方便验证；生产可以按业务规则设置 15 分钟、30 分钟或更长。

### 46.10 测试覆盖

本轮补充和调整了以下测试：

```text
VoucherOrderServiceImplTest
VoucherServiceImplTest
SeckillVoucherServiceImplTest
```

覆盖重点包括：

- 秒杀入口执行 Lua 并返回订单 ID。
- Lua 返回库存不足、重复下单、活动未准备好、未开始、已结束时映射正确文案。
- Redis 执行异常时接口返回失败，不触发 DB 库存扣减。
- 异步落库成功后投递超时关闭任务。
- DB 库存不足时事务失败。
- 重复订单按幂等成功处理。
- Stream 消息落库成功后 ACK。
- 落库失败时不 ACK。
- 非法消息和初始化消息会跳过并 ACK。
- pending 超过最大投递次数后进入死信 Stream。
- 未支付订单超时关闭会回补 MySQL 和 Redis。
- 已支付订单不会被超时关闭。
- 支付成功只允许本人订单、未支付状态。
- 已取消订单不能支付。
- 新增秒杀券会预热 stock、begin、end Redis key。
- 启动预热使用 `setIfAbsent` 写 stock，不覆盖 Redis 实时库存。

验证命令：

```bash
mvn test "-Dtest=VoucherOrderServiceImplTest,VoucherServiceImplTest,SeckillVoucherServiceImplTest"
mvn test
```

当前全量测试结果为：

```text
Tests run: 107, Failures: 0, Errors: 0, Skipped: 0
```

### 46.11 核心优化思路总结

这次优惠券与秒杀订单模块的核心思路可以总结为六点。

第一，高并发入口要前置到 Redis，但不能把 Redis 当作唯一事实源。秒杀入口用 Redis Lua 扛并发、扣库存、判重、写消息；最终订单和库存仍要落到 MySQL，由 MySQL 唯一索引和条件更新保证最终正确性。

第二，库存扣减和一人一单必须原子化。秒杀最怕多次网络请求之间出现并发穿插，因此库存判断、扣减、用户下单标记和写 Stream 放进同一个 Lua 脚本里执行。

第三，异步链路必须按 at-least-once 设计。Redis Stream 消息可能重复投递，消费者可能中途失败，所以不能依赖“只消费一次”的假设，而要通过数据库唯一索引、幂等处理和 ACK 时机控制保证可靠。

第四，订单状态流转要用条件更新表达。支付成功和超时关闭都从 `status=1` 出发，谁先更新成功谁获得状态，后续请求自然失败或跳过。这比加分布式锁更简单，也更贴近订单领域建模。

第五，延迟任务必须有兜底。Redisson 延迟队列适合当前单体项目体量，但不能只依赖它。定时扫表补偿让系统即使在队列异常、服务重启、延迟消息丢失时，也能从 MySQL 状态恢复业务。

第六，缓存预热要避免覆盖实时数据。启动时补齐 begin/end key 是安全的，但 stock 是高频变化数据，因此用 `setIfAbsent`，只在 Redis stock 缺失时从 MySQL 恢复，避免重启时把实时库存回滚。

### 46.12 后续还可以继续增强的方向

当前优惠券秒杀模块已经形成了一版适合当前项目体量的生产化闭环。如果继续向更大规模生产场景推进，可以考虑以下方向。

- 增加管理端秒杀券预热/重建接口，支持指定 voucherId 或全量重建 Redis 秒杀 key。
- 增加订单对账任务，对比 `tb_seckill_voucher.stock`、`tb_voucher_order` 和 Redis 库存，发现偏差后记录或修复。
- 给 `stream.orders.dead` 增加后台查询和人工重放能力，便于处理长期失败订单消息。
- 支付接口接入真实支付渠道回调，当前 `payVoucherOrder` 更像是支付成功状态机入口。
- 增加取消订单接口，让用户主动取消未支付订单，并复用当前 `closeUnpaidVoucherOrder` 的库存回补能力。
- 秒杀入口增加限流和风控，例如用户维度、IP 维度、设备指纹维度限流。
- 秒杀券更新、下架、过期时同步刷新或删除 Redis 预热 key。
- 将 Redis Stream 消息发布和消费封装成独立组件，避免 `VoucherOrderServiceImpl` 同时承担入口、消费者、超时关闭和补偿扫描。
- 当订单、支付、通知、积分等链路拆成多个服务后，再考虑把 Redis Stream/Redisson 延迟队列替换为 RocketMQ 或 RabbitMQ。

因此，当前优惠券秒杀模块已经不只是“能抢券、能下单”，而是形成了 Redis 高并发入口、Lua 原子扣减、Stream 异步削峰、MySQL 最终一致、订单状态机、超时关闭、库存回补、死信处理、配置化和启动预热的一套完整闭环。对于当前应用体量来说，这比直接引入更重的消息队列更合适；后续如果业务链路继续扩张，再逐步抽象消息层和引入更专业的 MQ 会更自然。

## 47. 异步 AI 任务系统 Phase 1 改造记录

本轮记录仅覆盖当前项目的异步 AI 任务系统 Phase 1 改造。

这次改造把管理端 RAG 重建从纯同步接口扩展为“提交任务 + 后台执行 + 轮询查询”的异步模型，同时保留原有同步接口，避免影响已有调用方。

### 47.1 本轮目标

原有管理端 RAG 重建接口会在 HTTP 请求线程中直接执行全量或单店重建。随着评价数量和向量索引规模增长，这类操作会变成典型长任务：

- 请求容易超时。
- 管理端难以展示任务状态。
- 重复点击可能触发重复重建。
- 后续如果接入批量总结、运营报表、长分析，也需要统一任务模型。

因此本轮先落地 Phase 1，不引入新依赖，直接复用已存在的 Redisson 能力。

### 47.2 新增任务模型

任务状态模型放在 `com.hmdp.dto.ai`：

```text
AiTaskStatus
AiTaskType
AiTask
```

`AiTaskStatus` 表达任务生命周期：

```text
PENDING
RUNNING
SUCCESS
FAILED
CANCELLED
```

`AiTaskType` 当前只接入两类管理端 RAG 任务：

```text
RAG_REBUILD_ALL
RAG_REBUILD_SHOP
```

`AiTask` 使用 `long epochMillis` 保存创建和更新时间，避免引入额外 Jackson JavaTime 配置；`params` 保留任务参数，`result` 保存同步重建逻辑返回的 `ShopRagRebuildResult`，失败时写入 `errorMessage`。

### 47.3 Redisson 存储与队列

任务系统代码集中在 `com.hmdp.ai.task`：

```text
AiTaskRepository
AiTaskQueue
AiTaskService
AiTaskWorker
```

配置项位于：

```yaml
hmdp:
  ai:
    task:
      enabled: true
      worker-threads: 2
      result-ttl-hours: 24
      queue-key: hmdp:ai:task:queue
      bucket-prefix: hmdp:ai:task:
```

当前使用的 Redis key 语义为：

- `hmdp:ai:task:queue`：Redisson `RBlockingQueue<String>`，保存待执行任务 ID。
- `hmdp:ai:task:{taskId}`：任务 JSON 结果桶，默认保留 24 小时。
- `hmdp:ai:task:inflight:{dedupKey}`：在途任务去重桶，任务结束后清理。

Phase 1 选择 `RBlockingQueue` 是为了保持实现简单：Redis 持久化队列、服务重启不丢待消费 ID、多实例下 `take` 可以自然分担。RAG 重建本身具备幂等语义，单次崩溃丢失执行中的任务在当前阶段可接受；如果后续需要严格 at-least-once、重投递、死信和消费组语义，再升级为 Redis Stream。

### 47.4 提交与去重

`AiTaskService` 对外提供任务提交和查询能力：

```text
submit(type, params, ownerUserId)
get(taskId)
```

提交时会按以下信息生成去重 key：

```text
type + normalizedParams + ownerUserId
```

如果同一个用户对同一类任务和同一组参数重复提交，且已有任务仍处于在途状态，则直接返回已有 `taskId`，不会重复入队。这样管理端重复点击或网络重试不会制造重复重建任务。

### 47.5 后台 Worker

`AiTaskWorker` 在应用启动后按配置创建 daemon worker 线程，循环从 Redisson 队列中 `take` 任务 ID。

执行流程：

```text
PENDING -> RUNNING -> SUCCESS / FAILED
```

Worker 不重写 RAG 重建逻辑，只调用已有同步方法：

```text
ShopReviewVectorIndexService.rebuildAll(shopLimit, perShopLimit)
ShopReviewVectorIndexService.rebuildShop(shopId, limit)
```

这保证了同步接口和异步任务使用同一套底层重建逻辑，减少行为漂移。

### 47.6 新增管理端接口

旧同步接口继续保留：

```text
POST /api/shop-summary/admin/rag/rebuild
POST /api/shop-summary/admin/rag/shops/{shopId}/rebuild
```

新增异步任务接口：

```text
POST /api/shop-summary/admin/rag/tasks/rebuild
POST /api/shop-summary/admin/rag/tasks/shops/{shopId}/rebuild
GET  /api/shop-summary/admin/rag/tasks/{taskId}
```

三个新接口都继续使用：

```text
ai:rag:manage
```

提交接口返回：

```json
{
  "taskId": "..."
}
```

查询接口返回完整 `AiTask`，包含 `status`、`params`、`result`、`errorMessage` 和时间戳。

### 47.7 边界约束

新任务包保持在 AI 边界内：

```text
com.hmdp.ai.task
```

该包不直接依赖：

```text
com.hmdp.mapper.*
com.hmdp.entity.*
com.hmdp.controller.*
com.hmdp.service.impl.*
com.hmdp.event.*
ShopStatsService
```

任务执行层只依赖 AI 内部的 `ShopReviewVectorIndexService` 与 `AiMetricsService`，对外暴露 DTO 与服务门面。

### 47.8 测试覆盖

本轮新增和扩展的测试包括：

```text
AiTaskServiceTest
AiTaskWorkerTest
ShopAIRagAdminControllerTest
```

覆盖重点：

- 新任务提交会保存 `PENDING` 任务并入队。
- 在途去重命中时返回已有 `taskId`，不会重复入队。
- Worker 成功执行时写入 `SUCCESS` 和 `result`。
- Worker 捕获运行时异常时写入 `FAILED` 和 `errorMessage`。
- 管理端异步提交和查询接口路径、权限注解、返回结构保持正确。

验证命令：

```bash
mvn -q compile
mvn -q "-Dtest=AiModuleBoundaryTest" test
mvn test
```

当前全量测试结果为：

```text
Tests run: 258, Failures: 0, Errors: 0, Skipped: 0
```

### 47.9 后续方向

Phase 1 已经能支撑管理端 RAG 重建异步化。后续可以继续增强：

- 增加任务进度上报，让 `progressCurrent/progressTotal` 真正参与前端展示。
- 增加 SSE 或 WebSocket 推送，减少管理端轮询成本。
- 增加任务取消能力，补齐 `CANCELLED` 状态语义。
- 将用户级长分析、批量总结、运营报表接入同一任务模型。
- 如果任务可靠性要求提高，将 `RBlockingQueue` 升级为 Redis Stream 消费组，并增加重试、死信和人工重放。

## 48. 第三批工程收尾记录

本轮作为当前阶段最后一批工程收尾，聚焦可靠性、性能和运维可观测性，不改变接口 URL、`Result` 外壳、权限体系，也不引入新的重量级依赖或推进 AI 微服务拆分。项目最终定位保持为：AI 增强型本地生活平台，在单体内抽取清晰 AI 子系统，通过 Port/Adapter 和 ArchUnit 约束预留后续按需服务化边界，高并发秒杀与缓存治理成型，安全与数据一致性完成阶段性收束。

本轮完成四类治理：

- 秒杀订单消费者线程和超时关闭线程配置化，并增加 Redis Stream ready 门控；默认 `stream-required=true` 时 Stream/consumer group 未就绪会快速拒绝下单，不继续执行 Lua 静默接单。
- AI task 增加 `startedAtEpochMillis`、`heartbeatAtEpochMillis`、`finishedAtEpochMillis`、`retryCount`，并通过定时 stuck 扫描把超时 RUNNING 任务重入队或标记失败；最大重试次数后失败收敛，仍保留 Redis BlockingQueue 架构，未升级为 Redis Stream 是本轮有意控制范围。
- 附近店铺 GEO 查询在存在 keyword、area、score、price、openNow 等业务过滤时，会按 `initial-fetch-multiplier`、`fetch-growth-factor`、`max-scan-size` 循环扩大候选窗口，减少过滤后分页不足，并用最大扫描上限防止无限扫描。
- CacheClient 互斥锁超时后先短暂 retry 读取缓存，默认 `fallback-to-db=false`，未命中时抛出 `CacheBusyException` 保护数据库；店铺详情将该状态映射为 `SYSTEM_BUSY`，避免误报店铺不存在。

新增关键配置：

```yaml
hmdp:
  voucher:
    order:
      worker-threads: 2
      close-worker-threads: 1
      stream-required: true
      stream-health-check-enabled: true
  ai:
    task:
      heartbeat-interval-seconds: 30
      running-timeout-minutes: 30
      max-retry-count: 3
      stuck-scan-enabled: true
      stuck-scan-fixed-delay-millis: 60000
      stuck-scan-limit: 100
  shop:
    nearby:
      initial-fetch-multiplier: 3
      fetch-growth-factor: 2
      max-scan-size: 200
  cache:
    mutex:
      retry-after-fail:
        enabled: true
        max-attempts: 3
        sleep-millis: 50
        fallback-to-db: false
```

需要人工结合部署规模确认的配置包括：`voucher.order.worker-threads`、`voucher.order.stream-required`、`ai.task.running-timeout-minutes`、`shop.nearby.max-scan-size`、`cache.mutex.retry-after-fail.fallback-to-db`。生产建议保持 Stream required 和缓存锁超时不直打 DB 的默认保护策略。当前状态可以表述为具备生产化设计思路、完成单体内模块化和可靠性收束；后续是否物理服务化应按流量、团队和部署复杂度再决定。
